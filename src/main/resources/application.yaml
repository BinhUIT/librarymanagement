server:
    port: 8080
    servlet:
        context-path: /Test
        encoding:
            charset: UTF-8 
            

spring:
    config:
        import: optional:file:.env[.properties]
    datasource:
        url: jdbc:mysql://localhost:3306/LIBRARY
        username: root
        password: your_password
    jpa:
        hibernate:
            ddl-auto: none
            naming:
                implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyComponentPathImpl
                physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
        show-sql: true
    mail:
        host: smtp.gmail.com
        port: 587
        username: leyen15121971@gmail.com
        password: ijxl pylv ektk fbmi
        properties:
            mail:
                smtp:
                    auth: true
                    starttls:
                        enable: true
    
    



